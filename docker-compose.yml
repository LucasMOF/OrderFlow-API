services:
  order-db:
    image: postgres:16
    environment:
      POSTGRES_USER: lucas
      POSTGRES_PASSWORD: 1234
      POSTGRES_DB: orderdb
    ports:
      - "5432:5432"
    volumes:
      - postgres-order-data:/var/lib/postgresql/data
volumes:
  postgres-order-data: